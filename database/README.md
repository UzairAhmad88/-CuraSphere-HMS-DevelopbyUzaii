# Database

PostgreSQL is the system of record.

Use EF Core migrations as the primary schema evolution mechanism.
SQL scripts in this folder are for supporting operations, seed/reference data and DBA tasks.

Never place production credentials in this repository.
